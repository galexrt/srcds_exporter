machine:
  environment:
    PATH: "/usr/local/go/bin:/usr/local/go_workspace/bin:~/.go_workspace/bin:${PATH}"

dependencies:
  override:
    - mkdir -p ~/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME}
    - ln -s ${HOME}/${CIRCLE_PROJECT_REPONAME} ${HOME}/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}
    # './...' is a relative pattern which means all subdirectories
    - go get github.com/mitchellh/gox
    - go get github.com/aktau/github-release
    - go get -t -d -v ./...
    - gox -output="srcds_exporter_{{.OS}}_{{.Arch}}" -verbose ./...
    - find . -name "srcds_exporter_*" -exec sh -c 'github-release upload --user ${CIRCLE_PROJECT_USERNAME} --repo ${CIRCLE_PROJECT_REPONAME} --tag ${CIRCLE_BRANCH} --draft --name "${1/.\/}" --file "$1"' _ {} \;

test:
  override:
    # './...' is a relative pattern which means all subdirectories
    - go test -v -race ./...
