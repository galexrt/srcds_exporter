machine:
  environment:
    PATH: "/usr/local/go/bin:/usr/local/go_workspace/bin:~/.go_workspace/bin:${PATH}"

dependencies:
  override:
    - mkdir -p ~/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME} ./output
    - ln -s ${HOME}/${CIRCLE_PROJECT_REPONAME} ${HOME}/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}
    - go get github.com/mitchellh/gox
    - go get github.com/tcnksm/ghr
    - go get -t -d -v ./...
    - gox -output="./output/srcds_exporter_{{.OS}}_{{.Arch}}" -verbose ./...
    - ghr -draft -delete master-build ./output
    - for file in ./output/srcds_exporter_*; do github-release upload -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -t ${CIRCLE_BRANCH}-build -n "${file}" -f "${file}"; done

test:
  override:
    - go test -v -race ./...
